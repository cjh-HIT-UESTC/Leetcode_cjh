对于本题，逻辑很简单，末尾有9就加一进位，否则停止跳出
唯一需要注意的是如：999这种情况，会输出1000，从3位数组变成了4位数组了
因为我不熟悉，所以我一开始先定义了一个全为0的数组，再把首位置0
但是这道题可以这样：
    digits.at(i)，表示第i位（和[i]是同一种效果）
    digits.inset（digits.begin(),1）  在首位插入
    
    
    basic_string& insert (size_type pos, const basic_string& str);
        在原串下标为pos的字符前前前前前前前前前前前前前前前前前前前前前前前前前前插入字符串str
        
       这样就能实现多一位了
       
    本题还要注意的一个地方是：
      对于数组的初始化，可以
      vector<int>A(n)  ，这样n个数全变为了0
      vector<int>A(n，1)  ，这样n个数全变为了1
      vector<int> ilist = {1,2,3.0,4,5,6,7};  
      
      
      如果是这样：vector<int> ilist1;
      默认初始化，vector为空， size为0，表明容器中没有元素，而且 capacity 也返回 0，意味着还没有分配内存空间。这种初始化方式适用于元素个数未知，需要在程序中动态添加的情况。
      注意：它里面没有元素！！！！所以不可以直接索引！！！
