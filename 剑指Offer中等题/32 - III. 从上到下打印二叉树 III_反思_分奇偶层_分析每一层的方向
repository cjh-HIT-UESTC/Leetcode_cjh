对于本题 最重要的是分析清楚每层的队列运动方向：    
                                    3 向前出 从后按左右进
                                   9 20 向后出 从前按右左进
                               4 5 15 7 向前出 从后按左右进
                        这样就能模拟出来了
                        
   对于打印二叉树：
      1、首先是广搜，要用队列辅助   队列存放的数据类型要注意
      2、然后 分层的话 可以利用一个for循环 在每一层的队列里 让那一层的点全部出去就行了  利用  queue.size()
      3、对于之 字形 只需要分析清楚运动方向即可  
      4、对于二维数组向量  往里面存放数据要用一个一维向量数组来辅助放 利用push_back
      
