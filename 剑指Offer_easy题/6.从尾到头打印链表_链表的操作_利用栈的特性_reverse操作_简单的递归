首先 先复习链表的操作 head为头指针（作为一个不断在变的指针）
判断条件是指针不指向空，head!=null 或者!head 都行
push_back函数是把元素放到vector最后面 最后reverse函数反向 reverse(ret.begin(), ret.end());

然后因为上述步骤用来反向函数 如果不调用函数就可以 利用栈 stack 先把每个元素放进栈中 然后再依次pop出来到vector 
其实要不是返回必须为数组 还可以利用队列 但是我现在不熟悉队列

第三种用递归  相当于先把它递归到最后一个元素 然后把它放在数组最前面 然后依次反向放置
需要注意的是 这里在递归里的指针是指向next的  但是在后面的push_back是指向head的 这样就能实现错位
