对于本题 我一开始想的使用erase函数和insert函数 插入和删除 但是由于反复调用函数 会导致超时
函数用法：
s.erase(i,1);   第i的位置开始，删除一个元素(注意是i位置开始，包含它自己)
s.insert(i,"%20");   第i的位置 之前！！，插入字符串，也可以插入字符‘’
s.replace("","%20")   把队列中与给定值相等的所有值替换为另一个值,空格换了

也可以，也是本题精髓：用
for(auto &c :s) 遍历字符串S中的每一个字符**************************************
然后比较，并定义一个新的字符串，然后用push_back函数给他不断地放入新的字符即可
for(auto &c:s)
        {
            if(c==' ')
            {
                s1.push_back('%');
                s1.push_back('2');
                s1.push_back('0');
            }
            else
            {
                s1.push_back(c);
            }
        }
