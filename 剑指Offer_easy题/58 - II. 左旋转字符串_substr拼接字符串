class Solution {
public:
    string reverseLeftWords(string s, int n) {
        int len=s.size();
        for(int i=0;i<n;++i)
        {
            s+=s[i];
        }
        return s.substr(n,len);
    }
};
