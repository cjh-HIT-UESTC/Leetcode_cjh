首先本题需要的是栈的操作：
  栈的定义：stack<int>stack1;
  放入栈顶：stack1.push(int)
  删除栈顶：stack1.pop()  （没有栈顶就不能pop）
  判空操作:stack1.empty()
  
第二 对于本题这种结构体 要返回null，不用写什么，它自己就返回null

最后是本题的逻辑：两个栈互相倒，但是为了节约时间空间，可以先判断栈2里是否为空，不为空就放入栈1，不倒，出栈2顶，
                为空，就判断栈1是否为空，不为空就把栈1倒入栈2，并出栈2顶，为空就说明无元素，返回-1
