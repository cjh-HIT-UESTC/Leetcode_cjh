第一种：直接暴力法：用两层循环去找是否有相同

第二种：先用sort函数：这里用法   sort(nums.begin(),nums.end())
      排完序之后，用一次循环，i，i+1即可找到
     
第三种：标记法：每碰到一个数，就把这个数i指向的数组的位置i的数置为负数，然后在每次这样的操作前加上一个判断，如果在翻转前已经是负数了，那么它的位置数 就是那个重复的数字
******************************************第三种方法的思想很重要*************************************************************************

第四种，把数组看作一个栈，每次pop一个到一个哈希数组里，在每次上进去之前先判断哈希数组里面是否有了
